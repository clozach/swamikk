# Contextual feedback and approved native changes

The API persists feedback and change proposals. Optional private feedback mailbox notifications use the existing durable outbox; saving feedback succeeds independently of notification delivery. No model provider is invoked. A signed-in operator can copy a feedback prompt, prepare a proposal through the existing Codex workflow and POST it from the site session. An API key does not authenticate these endpoints. Feedback strings and display locators remain data; neither is executed.

Shared request/response types live in `packages/common-models/src/{feedback,content-change}.ts`. Route OpenAPI fragments live next to `/api/feedback` and `/api/content-changes`. All writes require the current site's `Origin` and `Content-Type: application/json`. Bodies are counted while reading, including chunked requests. Mongo counters enforce fixed-window limits across processes; they retain hashed bucket keys, not raw IPs.

## Operator proof

1. Submit a comment as a visitor, then as a member. Verify the member sees only her own feedback and cannot attach photos or obtain a preparation prompt. Feedback never changes content.
2. Sign in with `site:manage` or `course:manage_any` to read the private site inbox. Photos must belong to the same site's MediaLit group. `course:manage` alone can prepare and approve lessons it owns through native permission checks, but does not grant the whole-site inbox.
3. Prepare a text lesson proposal with a title and/or safe ProseMirror text patch. Read the returned before/after preview, live/draft context (`baseline.published`), version and preview hash. Verify the lesson remains unchanged.
4. Approve with that version and preview hash. Inspect `state.kind`, not just HTTP status. Repeating the approval cannot perform another content write. A revised proposal invalidates the older approval.
5. Change the lesson in the legacy editor before approval: approval returns `stale` without overwriting it. Native saves use a full-state atomic comparison plus a monotonically increasing revision; a document loaded before the approved write cannot later save over it.
6. Interrupt an apply. Reconcile the existing proposal. A receipt proves a committed edit. Otherwise reconciliation either proves the old write cannot commit or atomically advances only the revision and records cancellation, preventing a delayed request from writing. Database/read ambiguity retains `applying`/`uncertain` plus the target lock. Refresh and reconcile; never manufacture success or replay a new operation against an uncertain baseline.
7. From an applied change, prepare a recovery proposal. This does not restore content until separately approved. Any subsequent lesson edit prevents this automatic reverse operation, preserving later work.

The first adapter supports text lessons and text structure. New embedded HTML/assets are rejected; existing opaque/media nodes must be retained verbatim. Native validation and media handling run on application. If media processing changes the proposed JSON, the edit is not written and requires content-editor review. The page-field adapter below extends this same proposal contract. Drip changes have their own consequence-aware service and do not pass arbitrary native writes through this API.

## Persistence and cleanup

- `ContextualFeedback`: open records remain until handled. Closing assigns a 90-day TTL; reopening removes expiry. The member who submitted a record, or an inbox administrator, can DELETE it. Anonymous creation returns only its receipt and provides no anonymous read/list endpoint. Admin photo IDs reference existing library assets; deleting feedback does not delete those assets.
- `ContentChange`: no automatic expiry. Applied baselines, approvals and revision history are recovery/audit records. The API can remove only never-applied settled proposals. Applying/uncertain records retain a unique per-target lock and cannot be removed. Each proposal permits 20 revisions; start a new proposal after that. Approval records survive failed/stale outcomes.
- `FeedbackRateLimit`: hashed fixed-window counters expire after the next window. They contain no feedback text, email or raw IP.
- Account deletion calls `deleteUserFeedback(domainId, userId)` from `graphql/users/helpers.ts` through the session-independent `personal-data.ts` module. Proposal actor IDs remain as historical audit references; names and email addresses are not copied into proposal metadata.
- Tenant deletion integration: stop that tenant's writes/in-flight requests, reconcile outstanding operations, then call `deleteTenantContentChangeData(domainId)` before removing the tenant. It refuses cleanup with unresolved operations. These helpers are deliberately not exposed as public mutation endpoints.

The OpenAPI fragments are registered. The shared ? control supports held Ctrl/⌘ selection, a touch/keyboard chooser, retained tab drafts, admin-only page prompt collation and private photo uploads. `/dashboard/changes` renders before/after, consequences, approval/rejection, recovery and separately approved undo. `/dashboard/content` reads native products; the legacy tools have one removable Admin navigation entry. Both lists paginate with a timestamp-and-ID cursor; page prompt collation includes older feedback instead of silently stopping at50. Photo delivery rechecks admin, feedback attachment and tenant ownership and sends private no-store responses.

Model indexes must exist before taking traffic; `init()` gates the atomic counter and target claim. Tests use isolated Mongo memory instances and do not touch the running rig. Source checks use isolated services. Runtime rollout and visual review remain separate acceptance gates. Mailbox delivery is disabled until configured by a settings administrator; its UI distinguishes off, queued, delivered, failed and uncertain results.

## Page fields and native images

The shared ? chooser offers **Edit selected text or image** to `site:manage` outside Member Mimic. Authoring targets come from rendered native page/widget IDs, not from guessed routes or executed feedback selectors. `GET /api/content-changes/page-widget?pageId=…&widgetId=…` reads only the supported fields of an existing non-shared published block. Public/member comments keep their existing text-only flow; this editor is an administrator capability. All ContentChange routes reject active or expired Mimic cookies even without middleware.

Supported fields are rich-text block text; curriculum block title/description; an existing media block image; and the Anahata hero heading, existing button caption, existing paragraph text, banner fallback, wordmark and welcome photograph. Other blocks, layout/order, shared navigation, links, payments/access, new arbitrary embeds and new page structures require a different explicitly supported adapter. The simple rich-text control edits existing text leaves immediately while retaining marks, links and opaque material. A larger prompt can submit safe rich-text structure through the same proposal API. No embedded page content is executed as an instruction.

A page proposal uses `target: {kind: "page-widget", pageId, widgetId, field}` and one patch: `{kind: "text", text}`, `{kind: "rich-text", content}`, or `{kind: "image", mediaId, alt}`. It preserves the established version, previewHash, history, approvals, state and recovery actions. A browser cannot submit `restore-widget`; only recovery of an applied proposal creates it from its retained original settings. The editor retains unsent text/selected IDs in tab sessionStorage keyed by actor and exact field. Preparation stores a durable proposal but does not publish or alter Page/Domain content.

Images must be native same-tenant public PNG/JPEG/WebP/GIF/AVIF assets. The server resolves their media ID, verifies the authoritative group/access/type, seals the chosen library asset and checks it again before applying. Private feedback photos, paid media, arbitrary URL patches and SVG uploads are rejected. Both original and replacement media remain referenced by the proposal snapshots/history; the existing reference-aware collector protects recovery assets. Sealing is an explicit library-retention side effect of preparation, not a page publication.

Before/after use the native block renderer with frozen resolved settings and published theme/typefaces. Defaults are shown as the values actually reviewed while raw original settings remain available to recover the absence of an override. The hero's social rotation preview holds the reviewed fallback still; approval changes only that image and leaves rotation enabled. This is a selected-block preview, not a full page or unpublished site preview. It contains no working navigation controls. The preview's theme CSS is scoped to its own panels.

Approval atomically writes the selected published field, mirrors only that same field into an existing matching native draft, increments the native revision and writes an operation receipt. Any unpublished conflict in the selected field rejects preparation; every unrelated draft field stays unpublished. Full native page state and immutable document ID guard both native builder saves and approved application. Preview context changes invalidate approval. The unique unsettled target lock uses immutable Mongo document identity, so page renames cannot bypass it. Native deletion refuses a known pending change; disappearance during an interrupted write keeps an uncertain audit/lock rather than reporting a guessed failure or replaying it.

Receipt reconciliation proves application after lost responses. If a write did not occur, reconciliation fences the old revision; delayed work can no longer apply. Applied receipts survive later native builder saves. Recovery is a separate review and approval and requires the unchanged applied revision/selected settings/document identity. It restores default-derived settings without publishing unrelated draft work. Whole-page native publish remains a separate legacy action and can publish shared drafts; the single-field helper never calls it.

Focused proof covers native/approved save races, duplicate approvals, lost replies, cancellation fences, rename/recreation, authorization/origin/Mimic boundaries, private/changed media rejection, cross-tenant media references and default-derived recovery. The legacy Page update/publish/delete-block suite also runs. Other Page writers were reviewed: Course slug/name updates write only metadata and are detected by the full native filter; initialization uses insert-only defaults. None of those writers can replace the approved layout with an old loaded snapshot.

## New unpublished text pages

The Content overview offers **Propose a new text page** to active site administrators outside Member Mimic. It retains a prompt/intention and optional source material alongside the proposed final title, exact `/p/` address and body. This is an adapter for authored output, not in-app model execution. The small form accepts plain paragraphs; the existing proposal API also accepts the validated safe text structure. Public/member comments remain text-only and unchanged.

`target: {kind: "page-create", pageId}` pairs with `patch: {kind: "page-create", title, content, intent, materials}` and a summary. There is no feedback ID or arbitrary layout input in this adapter. Prepare/revise stores only a proposal with a preallocated immutable Page document ID, generated native widget IDs, frozen title/body/layout and the published appearance fingerprint. A revised version receives a new result ID. Unsent form text uses actor-keyed `page-edit:` tab storage and participates in the existing account-closure browser cleanup. Submitted prompt/materials stay in admin audit history; do not supply private material that should not be retained there.

The existing explicit version/hash approval claims a unique unsettled route lock, then calls the native `graphql/pages/approved-draft.ts` helper. This inserts a complete Page draft and creation receipt atomically, under the actual approving account's lifecycle reservation. It does not call native global publication, initialize shared chrome, grant access or enqueue messages. Real public GraphQL page reads and `/p/` route/metadata reads cannot see `draftOnly` pages. Native administrator lists include drafts and exclude erased records. Existing live pages without the new flag keep their prior behavior.

A creation receipt identifies the exact proposal/version/operation/hash/result. Lost responses are reconciled against that ID, without replaying creation. A missing result is cancelled by an insert-only erased tombstone at the same immutable Mongo identity; a delayed insert cannot then commit. An unavailable or conflicting receipt keeps the uncertain state and route lock. Tombstones have no page content and use a freed, reserved route. Native deletion of a created page erases its content but retains that identity and original receipt, so a late operation cannot recreate it. Applied proposal history and its source text remain retained audit; this is not backup erasure. Tenant teardown still requires all writes/in-flight requests to stop.

Historical result links include `documentId`, which native editor reads, saves, publish and delete-block operations check. Deleting a page and reusing its route cannot make an old proposal edit its replacement. Creation has no generic lesson/text undo button: later edits, deleting the draft and publication are explicit native actions.

**Publication is separate.** On the first publication of these pages, the native service reads current global drafts and refuses if the shared header/footer, theme or fonts differ from published state. The message tells the administrator to review and resolve those site-wide drafts separately. Successful first publication changes only the page and never promotes global drafts. The creation review makes this scope visible before approval. Existing live-page legacy publication behavior is unchanged; subsequent ordinary editor publication retains that legacy global scope.

Isolated tests cover REST/GraphQL/public-route visibility and metadata, permissions/origin/Mimic/closure, retained exact output, duplicate approvals, revision/hash/appearance staleness, before/after-prepare route collisions, missing/lost/uncertain results, a delayed insert losing to cancellation, erased identities, legacy pages and historical editor mutation guards. Browser-rendered native and hosted proof are separate root-owned release checks.
